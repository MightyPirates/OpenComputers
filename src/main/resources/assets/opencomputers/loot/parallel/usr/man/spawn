NAME
  parallel.spawn
  
CALLS
  parallel.spawn(fn)
  parallel.spawn(fn, arg1, arg2, ...)

RETURNS
  boolean: true, string: uid - if succeeded to create a new thread. 
  boolean: false, string: errmsg - if failed
  
DESCRIPTION
  It creates a new parallel thread and puts it at the end of other threads.
  It DOES NOT give control to the next thread.

EXAMPLES
  parallel.spawn(function() print("Hello world!") end)
  parallel.spawn(function(msg) print(msg) end, "Hello world!")
  
  
  
