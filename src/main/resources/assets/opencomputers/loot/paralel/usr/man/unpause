NAME
  paralel.pause
  
CALLS
  paralel.unpause(uid)
  paralel.unpause(uid, amount)
  
RETURNS
  boolean: true - if succeeded.
  boolean: false, string: errmsg - if failed.
  
DESCRIPTION
  Unpauses thread for a given amount (default = -1) or at most until it unpauses.
  coroutine.unpause(thread) has the same effect as coroutine.pause(thread, -1). 
  
EXAMPLES
  paralel.unpause(targetThreadUID)
  paralel.unpause(targetThreadUID, 3)
  paralel.unpause(targetThreadUID, math.huge)
